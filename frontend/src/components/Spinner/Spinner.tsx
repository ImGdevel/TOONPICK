import React from 'react';
import styles from './Spinner.module.css'; // CSS 모듈 경로를 적절히 수정하세요.

const Spinner: React.FC = () => {
  return <div className={styles.spinner}></div>;
};

export default Spinner; 